# Agent Dispatch v0.16.0 Compatibility Evidence

Date: 2026-08-06
Runner: local macOS development checkout, source CLI with
`AL_DEV_BYPASS_VERSION_DISPATCH=1`

## Installed versions

- Claude Code: `2.1.223 (Claude Code)`
- Codex CLI: `codex-cli 0.146.1`
- Antigravity: `1.1.10`

All installed versions meet or exceed the tested Agent Dispatch baselines:
Claude Code 2.1.207, Codex CLI 0.144.1, and Antigravity 1.1.1.

## Fresh and continuation probes

Each fresh probe used `al dispatch start --agent <provider> --prompt <token>`,
then `al dispatch wait <handle>`. Each continuation used `al dispatch continue
<handle> --prompt <token>`, then `al dispatch wait <handle>`. Every wait returned
`state: completed`, and each terminal result file contained exactly the requested
token.

| Provider | Handle | Fresh result | Continuation result | Result |
| --- | --- | --- | --- | --- |
| Claude | `wide-square-oscillator` | `claude-v016-release-fresh` | `claude-v016-release-continue` | Pass with newer-version warning |
| Codex | `small-steady-inductor` | `codex-v016-release-fresh` | `codex-v016-release-continue` | Pass with newer-version warning |
| Antigravity | `big-bright-transistor` | `antigravity-v016-release-fresh` | `antigravity-v016-release-continue` | Pass with newer-version warning |

The completed result files were written beneath `.agent-layer/tmp/runs/` before
each `wait` call returned. `al dispatch options` reported all three providers
available on this checkout.

## Approvals-mode delivery probe (new in v0.16.0)

v0.16.0 fixes headless dispatch approvals: `approvals.mode` is now delivered to
the provider, so a dispatched agent approved to run commands can actually edit
files. Before this release only `yolo` could write. The probes above run under
this repository's `yolo` configuration, so they do not exercise the fix; this
section does. Both providers whose approval delivery changed are covered.

Probe: a separate scratch repository initialized with `al init --no-wizard`, its
`[approvals] mode` set to `commands`, then
`al dispatch start --agent <provider> --prompt "create <file> containing <token>"`
followed by `al dispatch wait <handle>`.

| Provider | Handle | Expected artifact | Result |
| --- | --- | --- | --- |
| Claude | `short-steady-relay` | `claude-write.txt` containing `claude-v016-approvals-ok` | Pass — file written under `commands` |
| Codex | `nimble-square-relay` | `codex-write.txt` containing `codex-v016-approvals-ok` | Pass — file written under `commands` |

The Claude probe confirms `--permission-mode acceptEdits` reaches the provider
and that the allowlisted tools are delivered via `--allowedTools`: the write
succeeded without an interactive trust dialog. The Codex probe confirms
`sandbox_mode = workspace-write` reaches the provider: a `read-only` sandbox
would have failed on the first write.

### Codex credential scope when probing from a scratch repository

`agents.codex.local_config_dir` points `CODEX_HOME` at a repository-local
`.codex`, so Codex credentials are per-repository. A freshly initialized scratch
repository has `.codex/config.toml` but no `.codex/auth.json`, and a dispatch
there fails with a provider authentication error that is unrelated to the
approvals change under test. Two earlier attempts in this session failed for
exactly that reason.

Run the probe with `CODEX_HOME` pointed at an authenticated home. Agent Layer
honors a pre-set `CODEX_HOME` and warns when it differs from the repository-local
path, so no credential copying is required:

```bash
CODEX_HOME=/path/to/authenticated/.codex \
  al dispatch start --agent codex --prompt "..."
```

Note that a bare `codex exec` invocation does not reproduce this environment: with
no `CODEX_HOME` set it falls back to `~/.codex`, which is a different credential
store than the one Agent Dispatch uses.

## Safe Antigravity missing-ID behavior

`go test ./internal/agentdispatch -run
'TestAntigravity(SuccessfulAnswerWithoutIDIsNotResumable|ResumeWithoutParsedIDRetainsDurableMapping|LogIDIsStrictAndVersionGateFailsLoudly)$'
-count=1` passed on this checkout. The fixtures prove that an absent or changed
Antigravity UUID retains the diagnostic log and reports `not resumable`; it does
not query global or provider-private session state. A later continuation
preserves an existing durable mapping only when its provider conversation ID is
still proven.
