# Agent Dispatch v0.18.3 Compatibility Evidence

Date: 2026-08-30

Runner: local macOS checkout using a source build from the v0.18.3 candidate
commit. Provider probes ran in an isolated detached worktree so provider tools
could not modify the release checkout.

## Installed versions

- Claude Code: `2.1.250 (Claude Code)`
- Codex CLI: `codex-cli 0.151.0`
- Antigravity: `1.1.22`
- Grok CLI: `grok 1.0.5 (5115b46bc909) [stable]`

`al dispatch options` reported all four providers available. The configured
source-built targets were Codex `gpt-5.6-sol` with medium reasoning, Claude
`opus` with high reasoning, Antigravity `Gemini 3.7 Flash (High)`, and Grok
with high reasoning.

## Fresh and continuation probes

Each successful provider received a fresh `start`/`wait` probe and a
`continue`/`wait` probe on the same handle. Every successful terminal result
contained exactly the requested token, preserved the same provider
conversation ID, and included provider usage evidence.

| Provider | Handle | Provider conversation ID | Fresh result | Continuation result | Result |
| --- | --- | --- | --- | --- | --- |
| Codex | `calm-curved-transformer` | `01a055d9-6f9a-7d52-8559-2b6afb9f986a` | `probe-codex-0183-fresh` | `probe-codex-0183-continue` | Pass |
| Antigravity | `big-square-oscillator` | `08eda999-66f5-4713-ab28-e89dbaf71f0a` | `probe-antigravity-0183-fresh` | `probe-antigravity-0183-continue` | Pass |
| Grok | `slim-round-relay` | `435ebbfd-aadd-4a2d-96c1-280e5d90a5c5` | `probe-grok-0183-fresh` | `probe-grok-0183-continue` | Pass |
| Claude | `wide-bright-inductor` / `short-steady-capacitor` | None | Authentication failed: `Not logged in · Please run /login` | Authentication failed: `OAuth session expired and could not be refreshed` | Blocked by expired external OAuth session |

Claude was attempted through both fresh and continuation lifecycle calls, but
its external OAuth session was expired and could not be refreshed. No Claude
conversation ID or terminal result was available, so this evidence does not
claim Claude compatibility.

## Usage evidence

| Provider | Fresh usage | Continuation usage |
| --- | --- | --- |
| Codex | 12,578 input, 11,008 cached input, 13 output, 0 reasoning output tokens | 12,640 input, 12,032 cached input, 13 output, 0 reasoning output tokens |
| Antigravity | 14,817 input, 98 output, 86 thinking, 14,915 total tokens | 29,978 input, 182 output, 158 thinking, 30,160 total tokens |
| Grok | 668 input, 11,520 cache-read, 61 output, 48 reasoning, 12,249 total tokens | 146 input, 12,160 cache-read, 38 output, 25 reasoning, 12,344 total tokens |

## Safe structured-output behavior

The focused Antigravity safety tests passed on the same source checkout:

```text
go test ./internal/agentdispatch -run 'TestAntigravity(SuccessfulAnswerWithoutIDIsNotResumable|ResumeWithoutParsedIDRetainsDurableMapping|LogIDIsStrictAndVersionGateFailsLoudly)$' -count=1
```
