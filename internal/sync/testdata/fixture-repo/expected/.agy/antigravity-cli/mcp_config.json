{
  "mcpServers": {
    "agent-layer": {
      "command": "/bin/sh",
      "args": [
        "-c",
        "AL_MCP_WORKING_DIR=$PWD; export AL_MCP_WORKING_DIR; cd \"$1\" || exit; if [ -n \"$AL_DEV_BYPASS_VERSION_DISPATCH\" ] \u0026\u0026 [ -n \"$AL_DEV_EXECUTABLE\" ]; then exec \"$AL_DEV_EXECUTABLE\" dispatch mcp-server; else exec al dispatch mcp-server; fi",
        "agent-layer-mcp",
        "__REPO_ROOT__"
      ]
    },
    "example": {
      "serverUrl": "https://mcp.example.com?token=${AL_EXAMPLE_TOKEN}",
      "headers": {
        "Authorization": "Bearer ${AL_EXAMPLE_TOKEN}"
      }
    }
  }
}
