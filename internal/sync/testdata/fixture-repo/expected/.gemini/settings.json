{
  "_generatedBy": "agent-layer",
  "tools": {
    "allowed": [
      "run_shell_command(git status)",
      "run_shell_command(ls)"
    ]
  },
  "mcpServers": {
    "example": {
      "url": "https://mcp.example.com?token=${AL_EXAMPLE_TOKEN}",
      "headers": {
        "Authorization": "Bearer ${AL_EXAMPLE_TOKEN}"
      },
      "trust": true
    }
  }
}
