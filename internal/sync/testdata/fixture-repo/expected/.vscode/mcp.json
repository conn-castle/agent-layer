{
  "servers": {
    "example": {
      "type": "http",
      "url": "https://mcp.example.com?token=${env:AL_EXAMPLE_TOKEN}",
      "headers": {
        "Authorization": "Bearer ${env:AL_EXAMPLE_TOKEN}"
      }
    }
  }
}
