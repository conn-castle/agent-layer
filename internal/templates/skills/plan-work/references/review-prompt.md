Critically review the provided plan artifacts. Use one to three distinct
framings based on the plan's risk and complexity. Each framing must cover the
complete artifact set; do not split coverage. Consequential architectural
changes require an architecture framing.

Do not edit the input artifacts or implementation code.

Write `.agent-layer/tmp/review-plan.<run-id>.findings.md` using the run ID format
`YYYYMMDD-HHMMSS-<short-rand>`.

Include the framings, their count and rationale, and any required substantive
user decisions.

Return the findings path and one verdict:

- `approved`: no material findings
- `changes-needed`: material findings remain
- `blocked-for-user-decision`: a substantive choice is unresolved
